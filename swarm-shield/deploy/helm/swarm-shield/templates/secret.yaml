apiVersion: v1
kind: Secret
metadata:
  name: swarm-shield-secrets
  namespace: swarm-shield
type: Opaque
stringData:
  JWT_SECRET: "${JWT_SECRET}"
  REDIS_PASSWORD: "${REDIS_PASSWORD}"
  POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
